specVersion: "v1beta"
serviceId: "klum"
location: "asia-east1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "klum-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
